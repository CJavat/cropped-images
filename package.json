{
  "name": "cropped-images",
  "version": "1.0.0",
  "description": "Application to crop images and save them in another directory.",
  "main": "index.ts",
  "scripts": {
    "start": "npx tsc && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.7.6",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "inquirer": "^12.0.0",
    "sharp": "^0.33.5"
  }
}
